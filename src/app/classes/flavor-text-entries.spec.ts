import { FlavorTextEntries } from './flavor-text-entries';

describe('FlavorTextEntries', () => {
  it('should create an instance', () => {
    expect(new FlavorTextEntries()).toBeTruthy();
  });
});
